{
  "name": "calais-open-commons-protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate:data": "node scripts/validate-data.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "npm run validate:data && npm test && npm --prefix web run build"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "devDependencies": {
    "vinext": "^1.0.0-beta.9"
  }
}
